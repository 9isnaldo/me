---
interface Props {
	date: Date;
	clase: string
}

const { date,clase } = Astro.props;
---

<time  class={clase} datetime={date.toISOString()}>
	{
		date.toLocaleDateString('pt-BR', {			
			day: 'numeric',
			month: 'short',
			year: 'numeric',			
		})
	}
</time>